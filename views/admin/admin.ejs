<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Page</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            text-align: center;
        }

        h1 {
            margin-bottom: 20px;
            color: #333;
        }

        .form-container {
            background-color: #fff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            max-width: 400px;
            width: 100%;
        }

        form {
            display: flex;
            flex-direction: column;
        }

        label {
            margin-bottom: 5px;
            color: #333;
        }

        input {
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        button {
            padding: 10px;
            border: none;
            border-radius: 5px;
            background-color: #007BFF;
            color: white;
            cursor: pointer;
        }

        button:hover {
            background-color: #0056b3;
        }

        #instructions {
            margin-top: 20px;
            text-align: left;
        }

        #instructions h3 {
            margin-bottom: 10px;
        }

        #instructions ol {
            margin-left: 20px;
        }

        #instructions a {
            color: #007BFF;
        }
    </style>
    <script defer src="admin.js"></script>
</head>
<body>
    <div class="container">
        <h1>Welcome to Your Admin Page</h1>
        <% if (typeof error !== 'undefined') { %>
            <div style="color: red; margin-bottom: 10px;">
                <%= error %>
            </div>
        <% } %>
        <div id="login-form" class="form-container">
            <h2>Login</h2>
            <form id="loginForm" method="post" action="/admin/dashboard">
                <label for="email">email:</label>
                <input type="email" id="email" name="email" required><br><br>
                <label for="password">Password:</label>
                <input type="password" id="password" name="password" required><br><br>
                <button type="submit">Login</button>
            </form>
            <div id="instructions">
                <h3>How to get login username and password:</h3>
                <ol>
                    <li>Mail your travel agency details at <a href="mailto:infotour@tourismwebsite.com">infotour@tourismwebsite.com</a></li>
                    <li>We will mail you predefined username and password</li>
                    <li>Login using it</li>
                </ol>
            </div>
        </div>

        <!-- <div id="content-form" class="form-container" style="display: none;">
            <h2>Tour Package Details</h2>
            <form id="contentForm" enctype="multipart/form-data">
                <label for="org-name">Organization/Agency Name:</label>
                <input type="text" id="org-name" name="org-name" required><br><br>
                <label for="camp-place">Camp Place:</label>
                <input type="text" id="camp-place" name="camp-place" required><br><br>
                <label for="duration">Duration (days):</label>
                <input type="number" id="duration" name="duration" required><br><br>
                <label for="package-price">Package Price:</label>
                <input type="number" id="package-price" name="package-price" required><br><br>
                <label for="departure-place">Departure Place:</label>
                <input type="text" id="departure-place" name="departure-place" required><br><br>
                <label for="starting-date">Starting Date:</label>
                <input type="date" id="starting-date" name="starting-date" required><br><br>
                <label for="image">Upload Image:</label>
                <input type="file" id="image" name="image" required><br><br>
                <button type="submit">Submit</button>
            </form>
        </div> -->
    </div>

    <!-- <script>
        document.getElementById('loginForm').addEventListener('submit', function(event) {
            event.preventDefault();
            const username = document.getElementById('username').value;
            const password = document.getElementById('password').value;

            // Predefined username and password
            const predefinedUsername = 'admin';
            const predefinedPassword = 'password';

            if (username === predefinedUsername && password === predefinedPassword) {
                document.getElementById('login-form').style.display = 'none';
                document.getElementById('content-form').style.display = 'block';
            } else {
                alert('Login failed');
            }
        });

        document.getElementById('contentForm').addEventListener('submit', function(event) {
            event.preventDefault();
            const formData = new FormData(document.getElementById('contentForm'));
            
            fetch('/admin/tourdetails', {
                method: 'POST',
                body: formData
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    alert('Tour details submitted successfully');
                } else {
                    alert('Failed to submit tour details');
                }
            });
        });

        // Set the minimum date for the starting date input field to be tomorrow
        const startingDateInput = document.getElementById('starting-date');
        const today = new Date();
        const tomorrow = new Date(today);
        tomorrow.setDate(today.getDate() + 1);
        startingDateInput.min = tomorrow.toISOString().split('T')[0];
    </script> -->
</body>
</html>
